export const metadata = {
    title: 'Sign in',
    description: 'Authorization',
  }
  
  export default function RootLayout({ children }) {
    return (
      <div>
      {children}
      </div>
    )
  }
  